import React from 'react'

const NotesTemplate = ({title}) => {
  return (
    <div className="text-primaryColor font-titleFont text-center text-2xl font-semibold ">{title}</div>
  )
}

export default NotesTemplate